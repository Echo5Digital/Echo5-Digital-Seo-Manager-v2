# Frontend Environment Variables for Vercel
# Copy these to Vercel Dashboard → Settings → Environment Variables

# API Configuration (REQUIRED - Update with your Render backend URL)
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
NEXT_PUBLIC_SOCKET_URL=https://your-backend.onrender.com

# NextAuth (REQUIRED)
NEXTAUTH_URL=https://your-app.vercel.app
NEXTAUTH_SECRET=your-nextauth-secret-min-32-chars-generate-using-openssl

# Google OAuth (Optional)
GOOGLE_CLIENT_ID=your-google-oauth-client-id
GOOGLE_CLIENT_SECRET=your-google-oauth-client-secret

# ===========================================
# VERCEL-SPECIFIC INSTRUCTIONS:
# ===========================================
# 1. Go to Vercel Dashboard → Your Project
# 2. Click Settings → Environment Variables
# 3. Add each variable above:
#    - Variable Name: NEXT_PUBLIC_API_URL
#    - Value: https://your-backend.onrender.com (your Render backend URL)
#    - Environment: Production, Preview, Development
# 4. Generate NEXTAUTH_SECRET: 
#    Run: openssl rand -base64 32
# 5. Update NEXTAUTH_URL with your Vercel deployment URL
# 6. Save and redeploy

# ===========================================
# IMPORTANT NOTES:
# ===========================================
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - NEXTAUTH_SECRET must be different from JWT_SECRET
# - After adding env vars, redeploy your app
# - For local development, create .env.local with localhost URLs
